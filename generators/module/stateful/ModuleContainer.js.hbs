// @flow
import React, { Component } from 'react'
import { connect } from 'react-redux'

import {{properCase name}}View from './{{properCase name }}View'
import { action1, asyncAction1 } from './{{properCase name }}State'

type Props = {
  navigation: any,
  prop1: any,
  action1: Function,
  asyncAction1: Function
}

class {{properCase name}}Container extends Component<Props> {
  render() {
    return (
      <{{properCase name}}View
        {...this.props}
      />
    )
  }
}

// Map actions and state to props
const mapActions = dispatch => ({
  action1: param1 => dispatch(action1(param1)),
  asyncAction1: param1 => dispatch(asyncAction1(param1))
});
const mapState = state => ({
  prop1: state.{{camelCase name}}.prop1
});
export default connect(
  mapState,
  mapActions
)({{properCase name}}Container);
